# 待办事项 (TODO)

## ✅ 已修复 - VNA 驱动问题

### 2025-12-16 15:30 - 修复"该轨迹已存在"错误（事件号1623） ✅
- [x] **问题**：每次测量都执行 `CALC1:PAR:DEF:EXT` 创建轨迹，如果轨迹已存在会报错
- [x] **解决**：在创建轨迹前先执行 `:CALC:PAR:DEL:ALL` 删除所有旧轨迹
- [x] **增强**：添加数据验证，检查返回数据是否有效（频率和幅度至少2个点）

### 2025-12-16 12:40 - SCPI 指令更新 ✅
- [x] FDATA 直接作为 dB 幅度使用
- [x] 测量参数映射：SC11→'S11', SC22→'S22', IPWR→'Ipwr' 等
- [x] 所有SC和功率测量使用 `CUST:DEF` 指令
- [x] 混频器配置按7步骤流程

---

## 📋 修复记录

### siyi.py 修改
```python
# 在 get_measurement_data() 开头添加
print(f"  >> :CALC:PAR:DEL:ALL")
self.write(":CALC:PAR:DEL:ALL")

# 数据验证
if len(frequencies) < 2:
    return None, f"频率数据无效：只返回了 {len(frequencies)} 个点"
if len(data) < 2:
    return None, f"测量数据无效：只返回了 {len(data)} 个点"
```

---

## ⏳ 待用户补充
- [ ] 3674L 不兼容的 SCPI 指令清单

---

## 📝 更新时间
- 最后更新: 2025-12-16 15:30
- 修改文件: `backend/devices/siyi.py`
